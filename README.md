# tinyml-motion-classifier
TinyML Motion Classifier is an edge AI project that implements motion detection and classification using machine learning models optimized for embedded devices. The system collects sensor data, processes it using TinyML techniques, and performs real-time classification directly on edge hardware such as microcontrollers.
